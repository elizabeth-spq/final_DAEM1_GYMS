package isil.pe.springdatajpa.afiliacion;

import isil.pe.springdatajpa.visittas.VisittasDto;

public class AfilacioDto {
    /*
    private String code;
    private String message;
    private Object data;

    public static AfilacioDto whenAfiliacioDniAlreadyExists() {
        return AfilacioDto.().code("510").message("DNI ya existe").build();
    }

    public static VisittasDto whenError(String message) {
        return VisittasDto.builder().code("500").message("Something went wrong ".concat(message)).build();
    }

    public static VisittasDto whenRegistrationSucced() {
        return VisittasDto.builder()
                .code("200")
                .message("User registered successfully")
                .build();
    }
    
     */
}
